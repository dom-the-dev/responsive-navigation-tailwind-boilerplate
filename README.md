# Responsive Navigation with Tailwindcss - Boilerplate

### Installation
Install dependencies:
`yarn install` or `npm install`


### Development
Build CSS

`yarn build` or `npm run build`

Watcher

 `yarn watch` or `npm run watch`

