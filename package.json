{
  "name": "tailwind-responsive-navigation",
  "version": "1.0.0",
  "description": "Boilerplate for a HTML project with Tailwindcss",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tailwindcss -i ./src/style.css -o ./dist/style.css --watch",
    "build": "npx tailwindcss -i ./src/style.css -o ./dist/style.css"
  },
  "keywords": [],
  "author": "@Dom_TheDev",
  "license": "ISC",
  "dependencies": {
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.15"
  }
}
